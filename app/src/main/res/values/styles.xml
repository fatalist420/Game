<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorError">@color/colorError</item>
    </style>

    <style name="MyButtonBarTheme" parent="Widget.MaterialComponents.Button">
        <item name="android:layout_margin">2dp</item>
        <item name="android:layout_width">@dimen/my_button_bar_size</item>
        <item name="android:layout_height">@dimen/my_button_bar_size</item>
    </style>

    <dimen name="my_button_bar_size">32dp</dimen>

</resources>
